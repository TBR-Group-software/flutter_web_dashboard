part of '../../page.dart';

class _LocationPage extends StatelessWidget {
  const _LocationPage();

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('LOCATION'),
    );
  }
}
